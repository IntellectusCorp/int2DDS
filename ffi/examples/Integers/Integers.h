/**
 * Auto-generated by int2dds-idl from Integers.idl
 * DO NOT EDIT - changes will be overwritten
 */
#ifndef INTEGERS_IDL_H
#define INTEGERS_IDL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "int2dds_cdr.h"

/* ========================================================================
 * Type: IntegersType
 * Extensibility: APPENDABLE
 * ======================================================================== */

typedef struct IntegersType {
    int32_t id;
    int8_t i8_val;
    int16_t i16_val;
    int32_t i32_val;
    int64_t i64_val;
    uint8_t u8_val;
    uint16_t u16_val;
    uint32_t u32_val;
    uint64_t u64_val;
} IntegersType;

static inline void IntegersType_serialize_fields(
    Int2DdsCdrWriter *_w,
    const IntegersType *val)
{
    Int2DdsCdrWriter w = *_w;
    size_t dh = 0;
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_i8(&w, val->i8_val);
    int2dds_cdr_write_i16(&w, val->i16_val);
    int2dds_cdr_write_i32(&w, val->i32_val);
    int2dds_cdr_write_i64(&w, val->i64_val);
    int2dds_cdr_write_u8(&w, val->u8_val);
    int2dds_cdr_write_u16(&w, val->u16_val);
    int2dds_cdr_write_u32(&w, val->u32_val);
    int2dds_cdr_write_u64(&w, val->u64_val);
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    *_w = w;
}

static inline void IntegersType_deserialize_fields(
    Int2DdsCdrReader *_r,
    IntegersType *val_out)
{
    Int2DdsCdrReader r = *_r;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_i8(&r, &val_out->i8_val);
    int2dds_cdr_read_i16(&r, &val_out->i16_val);
    int2dds_cdr_read_i32(&r, &val_out->i32_val);
    int2dds_cdr_read_i64(&r, &val_out->i64_val);
    int2dds_cdr_read_u8(&r, &val_out->u8_val);
    int2dds_cdr_read_u16(&r, &val_out->u16_val);
    int2dds_cdr_read_u32(&r, &val_out->u32_val);
    int2dds_cdr_read_u64(&r, &val_out->u64_val);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    *_r = r;
}

static inline size_t IntegersType_serialize_cdr(
    const IntegersType *val,
    uint8_t *buf,
    size_t capacity,
    bool xcdr2)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, true, xcdr2);
    int2dds_cdr_write_encapsulation(&w, INT2DDS_CDR_APPENDABLE);
    size_t dh = 0;
    if (xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_i8(&w, val->i8_val);
    int2dds_cdr_write_i16(&w, val->i16_val);
    int2dds_cdr_write_i32(&w, val->i32_val);
    int2dds_cdr_write_i64(&w, val->i64_val);
    int2dds_cdr_write_u8(&w, val->u8_val);
    int2dds_cdr_write_u16(&w, val->u16_val);
    int2dds_cdr_write_u32(&w, val->u32_val);
    int2dds_cdr_write_u64(&w, val->u64_val);
    if (xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline bool IntegersType_deserialize_cdr(
    const uint8_t *buf,
    size_t len,
    IntegersType *val_out)
{
    Int2DdsCdrReader r;
    if (int2dds_cdr_reader_init(&r, buf, len) != INT2DDS_CDR_OK)
        return false;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_i8(&r, &val_out->i8_val);
    int2dds_cdr_read_i16(&r, &val_out->i16_val);
    int2dds_cdr_read_i32(&r, &val_out->i32_val);
    int2dds_cdr_read_i64(&r, &val_out->i64_val);
    int2dds_cdr_read_u8(&r, &val_out->u8_val);
    int2dds_cdr_read_u16(&r, &val_out->u16_val);
    int2dds_cdr_read_u32(&r, &val_out->u32_val);
    int2dds_cdr_read_u64(&r, &val_out->u64_val);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    return int2dds_cdr_reader_error(&r) == INT2DDS_CDR_OK;
}

static inline size_t IntegersType_serialize_key(
    const IntegersType *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, false, true); /* BE, XCDR2, no header */
    int2dds_cdr_write_i32(&w, val->id);
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline Int2DdsTypeInfo* IntegersType_type_info(void) {
    Int2DdsTypeInfo *ti;
    int2dds_type_info_create("IntegersType", 1, &ti);
    int2dds_type_info_add_field(ti, "id", INT2DDS_FIELD_INT32, 1);
    int2dds_type_info_add_field(ti, "i8_val", INT2DDS_FIELD_INT8, 0);
    int2dds_type_info_add_field(ti, "i16_val", INT2DDS_FIELD_INT16, 0);
    int2dds_type_info_add_field(ti, "i32_val", INT2DDS_FIELD_INT32, 0);
    int2dds_type_info_add_field(ti, "i64_val", INT2DDS_FIELD_INT64, 0);
    int2dds_type_info_add_field(ti, "u8_val", INT2DDS_FIELD_BYTE, 0);
    int2dds_type_info_add_field(ti, "u16_val", INT2DDS_FIELD_UINT16, 0);
    int2dds_type_info_add_field(ti, "u32_val", INT2DDS_FIELD_UINT32, 0);
    int2dds_type_info_add_field(ti, "u64_val", INT2DDS_FIELD_UINT64, 0);
    return ti;
}

#endif /* INTEGERS_IDL_H */
