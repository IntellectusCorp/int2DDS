/**
 * Auto-generated by int2dds-idl from Primitives.idl
 * DO NOT EDIT - changes will be overwritten
 */
#ifndef PRIMITIVES_IDL_H
#define PRIMITIVES_IDL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "int2dds_cdr.h"

/* ========================================================================
 * Type: PrimitivesType
 * Extensibility: APPENDABLE
 * ======================================================================== */

typedef struct PrimitivesType {
    int32_t id;
    bool bool_val;
    uint8_t byte_val;
    char char_val;
} PrimitivesType;

static inline void PrimitivesType_serialize_fields(
    Int2DdsCdrWriter *_w,
    const PrimitivesType *val)
{
    Int2DdsCdrWriter w = *_w;
    size_t dh = 0;
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_bool(&w, val->bool_val);
    int2dds_cdr_write_u8(&w, val->byte_val);
    int2dds_cdr_write_u8(&w, (uint8_t)val->char_val);
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    *_w = w;
}

static inline void PrimitivesType_deserialize_fields(
    Int2DdsCdrReader *_r,
    PrimitivesType *val_out)
{
    Int2DdsCdrReader r = *_r;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_bool(&r, &val_out->bool_val);
    int2dds_cdr_read_u8(&r, &val_out->byte_val);
    int2dds_cdr_read_u8(&r, (uint8_t*)&val_out->char_val);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    *_r = r;
}

static inline size_t PrimitivesType_serialize_cdr(
    const PrimitivesType *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, true, true);
    int2dds_cdr_write_encapsulation(&w, INT2DDS_CDR_APPENDABLE);
    size_t dh;
    int2dds_cdr_write_dheader_begin(&w, &dh);
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_bool(&w, val->bool_val);
    int2dds_cdr_write_u8(&w, val->byte_val);
    int2dds_cdr_write_u8(&w, (uint8_t)val->char_val);
    int2dds_cdr_write_dheader_finalize(&w, dh);
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline bool PrimitivesType_deserialize_cdr(
    const uint8_t *buf,
    size_t len,
    PrimitivesType *val_out)
{
    Int2DdsCdrReader r;
    if (int2dds_cdr_reader_init(&r, buf, len) != INT2DDS_CDR_OK)
        return false;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_bool(&r, &val_out->bool_val);
    int2dds_cdr_read_u8(&r, &val_out->byte_val);
    int2dds_cdr_read_u8(&r, (uint8_t*)&val_out->char_val);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    return int2dds_cdr_reader_error(&r) == INT2DDS_CDR_OK;
}

static inline size_t PrimitivesType_serialize_key(
    const PrimitivesType *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, false, true); /* BE, XCDR2, no header */
    int2dds_cdr_write_i32(&w, val->id);
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline Int2DdsTypeInfo* PrimitivesType_type_info(void) {
    Int2DdsTypeInfo *ti;
    int2dds_type_info_create("PrimitivesType", 1, &ti);
    int2dds_type_info_add_field(ti, "id", INT2DDS_FIELD_INT32, 1);
    int2dds_type_info_add_field(ti, "bool_val", INT2DDS_FIELD_BOOL, 0);
    int2dds_type_info_add_field(ti, "byte_val", INT2DDS_FIELD_BYTE, 0);
    int2dds_type_info_add_field(ti, "char_val", INT2DDS_FIELD_CHAR8, 0);
    return ti;
}

#endif /* PRIMITIVES_IDL_H */
