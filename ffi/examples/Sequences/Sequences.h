/**
 * Auto-generated by int2dds-idl from Sequences.idl
 * DO NOT EDIT - changes will be overwritten
 */
#ifndef SEQUENCES_IDL_H
#define SEQUENCES_IDL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "int2dds_cdr.h"

/* ========================================================================
 * Type: SequencesType
 * Extensibility: APPENDABLE
 * ======================================================================== */

typedef struct SequencesType {
    int32_t id;
    struct { bool* data; uint32_t length; } bool_seq;
    struct { uint8_t* data; uint32_t length; } byte_seq;
    struct { int32_t* data; uint32_t length; } i32_seq;
    struct { float* data; uint32_t length; } f32_seq;
    struct { double* data; uint32_t length; } f64_seq;
    struct { char (*data)[257]; uint32_t length; } string_seq;
    struct { int32_t data[10]; uint32_t length; } bounded_seq;
} SequencesType;

static inline void SequencesType_serialize_fields(
    Int2DdsCdrWriter *_w,
    const SequencesType *val)
{
    Int2DdsCdrWriter w = *_w;
    size_t dh = 0;
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_seq_header(&w, val->bool_seq.length);
    for (uint32_t _i = 0; _i < val->bool_seq.length; _i++) {
        int2dds_cdr_write_bool(&w, val->bool_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->byte_seq.length);
    for (uint32_t _i = 0; _i < val->byte_seq.length; _i++) {
        int2dds_cdr_write_u8(&w, val->byte_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->i32_seq.length);
    for (uint32_t _i = 0; _i < val->i32_seq.length; _i++) {
        int2dds_cdr_write_i32(&w, val->i32_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->f32_seq.length);
    for (uint32_t _i = 0; _i < val->f32_seq.length; _i++) {
        int2dds_cdr_write_f32(&w, val->f32_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->f64_seq.length);
    for (uint32_t _i = 0; _i < val->f64_seq.length; _i++) {
        int2dds_cdr_write_f64(&w, val->f64_seq.data[_i]);
    }
    { size_t _seq_dh = 0;
    if (w.xcdr2) { int2dds_cdr_write_dheader_begin(&w, &_seq_dh); }
    int2dds_cdr_write_seq_header(&w, val->string_seq.length);
    for (uint32_t _i = 0; _i < val->string_seq.length; _i++) {
        int2dds_cdr_write_string(&w, val->string_seq.data[_i]);
    }
    if (w.xcdr2) { int2dds_cdr_write_dheader_finalize(&w, _seq_dh); }
    }
    int2dds_cdr_write_seq_header(&w, val->bounded_seq.length);
    for (uint32_t _i = 0; _i < val->bounded_seq.length; _i++) {
        int2dds_cdr_write_i32(&w, val->bounded_seq.data[_i]);
    }
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    *_w = w;
}

static inline void SequencesType_deserialize_fields(
    Int2DdsCdrReader *_r,
    SequencesType *val_out)
{
    Int2DdsCdrReader r = *_r;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_seq_header(&r, &val_out->bool_seq.length);
    for (uint32_t _i = 0; _i < val_out->bool_seq.length; _i++) {
        int2dds_cdr_read_bool(&r, &val_out->bool_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->byte_seq.length);
    for (uint32_t _i = 0; _i < val_out->byte_seq.length; _i++) {
        int2dds_cdr_read_u8(&r, &val_out->byte_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->i32_seq.length);
    for (uint32_t _i = 0; _i < val_out->i32_seq.length; _i++) {
        int2dds_cdr_read_i32(&r, &val_out->i32_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->f32_seq.length);
    for (uint32_t _i = 0; _i < val_out->f32_seq.length; _i++) {
        int2dds_cdr_read_f32(&r, &val_out->f32_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->f64_seq.length);
    for (uint32_t _i = 0; _i < val_out->f64_seq.length; _i++) {
        int2dds_cdr_read_f64(&r, &val_out->f64_seq.data[_i]);
    }
    { uint32_t _seq_sz = 0; size_t _seq_sp = 0;
    if (r.xcdr2) { int2dds_cdr_read_dheader(&r, &_seq_sz, &_seq_sp); }
    int2dds_cdr_read_seq_header(&r, &val_out->string_seq.length);
    for (uint32_t _i = 0; _i < val_out->string_seq.length; _i++) {
        int2dds_cdr_read_string_copy(&r, val_out->string_seq.data[_i], 257, NULL);
    }
    if (r.xcdr2) { int2dds_cdr_read_dheader_end(&r, _seq_sz, _seq_sp); }
    }
    int2dds_cdr_read_seq_header(&r, &val_out->bounded_seq.length);
    for (uint32_t _i = 0; _i < val_out->bounded_seq.length; _i++) {
        int2dds_cdr_read_i32(&r, &val_out->bounded_seq.data[_i]);
    }
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    *_r = r;
}

static inline size_t SequencesType_serialize_cdr(
    const SequencesType *val,
    uint8_t *buf,
    size_t capacity,
    bool xcdr2)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, true, xcdr2);
    int2dds_cdr_write_encapsulation(&w, INT2DDS_CDR_APPENDABLE);
    size_t dh = 0;
    if (xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_seq_header(&w, val->bool_seq.length);
    for (uint32_t _i = 0; _i < val->bool_seq.length; _i++) {
        int2dds_cdr_write_bool(&w, val->bool_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->byte_seq.length);
    for (uint32_t _i = 0; _i < val->byte_seq.length; _i++) {
        int2dds_cdr_write_u8(&w, val->byte_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->i32_seq.length);
    for (uint32_t _i = 0; _i < val->i32_seq.length; _i++) {
        int2dds_cdr_write_i32(&w, val->i32_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->f32_seq.length);
    for (uint32_t _i = 0; _i < val->f32_seq.length; _i++) {
        int2dds_cdr_write_f32(&w, val->f32_seq.data[_i]);
    }
    int2dds_cdr_write_seq_header(&w, val->f64_seq.length);
    for (uint32_t _i = 0; _i < val->f64_seq.length; _i++) {
        int2dds_cdr_write_f64(&w, val->f64_seq.data[_i]);
    }
    { size_t _seq_dh = 0;
    if (w.xcdr2) { int2dds_cdr_write_dheader_begin(&w, &_seq_dh); }
    int2dds_cdr_write_seq_header(&w, val->string_seq.length);
    for (uint32_t _i = 0; _i < val->string_seq.length; _i++) {
        int2dds_cdr_write_string(&w, val->string_seq.data[_i]);
    }
    if (w.xcdr2) { int2dds_cdr_write_dheader_finalize(&w, _seq_dh); }
    }
    int2dds_cdr_write_seq_header(&w, val->bounded_seq.length);
    for (uint32_t _i = 0; _i < val->bounded_seq.length; _i++) {
        int2dds_cdr_write_i32(&w, val->bounded_seq.data[_i]);
    }
    if (xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline bool SequencesType_deserialize_cdr(
    const uint8_t *buf,
    size_t len,
    SequencesType *val_out)
{
    Int2DdsCdrReader r;
    if (int2dds_cdr_reader_init(&r, buf, len) != INT2DDS_CDR_OK)
        return false;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_seq_header(&r, &val_out->bool_seq.length);
    for (uint32_t _i = 0; _i < val_out->bool_seq.length; _i++) {
        int2dds_cdr_read_bool(&r, &val_out->bool_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->byte_seq.length);
    for (uint32_t _i = 0; _i < val_out->byte_seq.length; _i++) {
        int2dds_cdr_read_u8(&r, &val_out->byte_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->i32_seq.length);
    for (uint32_t _i = 0; _i < val_out->i32_seq.length; _i++) {
        int2dds_cdr_read_i32(&r, &val_out->i32_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->f32_seq.length);
    for (uint32_t _i = 0; _i < val_out->f32_seq.length; _i++) {
        int2dds_cdr_read_f32(&r, &val_out->f32_seq.data[_i]);
    }
    int2dds_cdr_read_seq_header(&r, &val_out->f64_seq.length);
    for (uint32_t _i = 0; _i < val_out->f64_seq.length; _i++) {
        int2dds_cdr_read_f64(&r, &val_out->f64_seq.data[_i]);
    }
    { uint32_t _seq_sz = 0; size_t _seq_sp = 0;
    if (r.xcdr2) { int2dds_cdr_read_dheader(&r, &_seq_sz, &_seq_sp); }
    int2dds_cdr_read_seq_header(&r, &val_out->string_seq.length);
    for (uint32_t _i = 0; _i < val_out->string_seq.length; _i++) {
        int2dds_cdr_read_string_copy(&r, val_out->string_seq.data[_i], 257, NULL);
    }
    if (r.xcdr2) { int2dds_cdr_read_dheader_end(&r, _seq_sz, _seq_sp); }
    }
    int2dds_cdr_read_seq_header(&r, &val_out->bounded_seq.length);
    for (uint32_t _i = 0; _i < val_out->bounded_seq.length; _i++) {
        int2dds_cdr_read_i32(&r, &val_out->bounded_seq.data[_i]);
    }
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    return int2dds_cdr_reader_error(&r) == INT2DDS_CDR_OK;
}

static inline size_t SequencesType_serialize_key(
    const SequencesType *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, false, true); /* BE, XCDR2, no header */
    int2dds_cdr_write_i32(&w, val->id);
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline Int2DdsTypeInfo* SequencesType_type_info(void) {
    Int2DdsTypeInfo *ti;
    int2dds_type_info_create("SequencesType", 1, &ti);
    int2dds_type_info_add_field(ti, "id", INT2DDS_FIELD_INT32, 1);
    int2dds_type_info_add_sequence_field(ti, "bool_seq", INT2DDS_FIELD_BOOL, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "byte_seq", INT2DDS_FIELD_BYTE, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "i32_seq", INT2DDS_FIELD_INT32, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "f32_seq", INT2DDS_FIELD_FLOAT32, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "f64_seq", INT2DDS_FIELD_FLOAT64, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "string_seq", INT2DDS_FIELD_STRING, 0, 0);
    int2dds_type_info_add_sequence_field(ti, "bounded_seq", INT2DDS_FIELD_INT32, 0, 0);
    return ti;
}

#endif /* SEQUENCES_IDL_H */
