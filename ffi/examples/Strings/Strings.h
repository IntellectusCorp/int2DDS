/**
 * Auto-generated by int2dds-idl from Strings.idl
 * DO NOT EDIT - changes will be overwritten
 */
#ifndef STRINGS_IDL_H
#define STRINGS_IDL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "int2dds_cdr.h"

/* ========================================================================
 * Type: StringsType
 * Extensibility: APPENDABLE
 * ======================================================================== */

typedef struct StringsType {
    int32_t id;
    char unbounded_str[257];
    char bounded_str[257];
} StringsType;

static inline void StringsType_serialize_fields(
    Int2DdsCdrWriter *_w,
    const StringsType *val)
{
    Int2DdsCdrWriter w = *_w;
    size_t dh = 0;
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_string(&w, val->unbounded_str);
    int2dds_cdr_write_string(&w, val->bounded_str);
    if (w.xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    *_w = w;
}

static inline void StringsType_deserialize_fields(
    Int2DdsCdrReader *_r,
    StringsType *val_out)
{
    Int2DdsCdrReader r = *_r;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_string_copy(&r, val_out->unbounded_str, 257, NULL);
    int2dds_cdr_read_string_copy(&r, val_out->bounded_str, 257, NULL);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    *_r = r;
}

static inline size_t StringsType_serialize_cdr(
    const StringsType *val,
    uint8_t *buf,
    size_t capacity,
    bool xcdr2)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, true, xcdr2);
    int2dds_cdr_write_encapsulation(&w, INT2DDS_CDR_APPENDABLE);
    size_t dh = 0;
    if (xcdr2) {
        int2dds_cdr_write_dheader_begin(&w, &dh);
    }
    int2dds_cdr_write_i32(&w, val->id);
    int2dds_cdr_write_string(&w, val->unbounded_str);
    int2dds_cdr_write_string(&w, val->bounded_str);
    if (xcdr2) {
        int2dds_cdr_write_dheader_finalize(&w, dh);
    }
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline bool StringsType_deserialize_cdr(
    const uint8_t *buf,
    size_t len,
    StringsType *val_out)
{
    Int2DdsCdrReader r;
    if (int2dds_cdr_reader_init(&r, buf, len) != INT2DDS_CDR_OK)
        return false;
    uint32_t obj_size = 0;
    size_t start_pos = 0;
    if (r.xcdr2) {
        int2dds_cdr_read_dheader(&r, &obj_size, &start_pos);
    }
    int2dds_cdr_read_i32(&r, &val_out->id);
    int2dds_cdr_read_string_copy(&r, val_out->unbounded_str, 257, NULL);
    int2dds_cdr_read_string_copy(&r, val_out->bounded_str, 257, NULL);
    if (r.xcdr2) {
        int2dds_cdr_read_dheader_end(&r, obj_size, start_pos);
    }
    return int2dds_cdr_reader_error(&r) == INT2DDS_CDR_OK;
}

static inline size_t StringsType_serialize_key(
    const StringsType *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, false, true); /* BE, XCDR2, no header */
    int2dds_cdr_write_i32(&w, val->id);
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline Int2DdsTypeInfo* StringsType_type_info(void) {
    Int2DdsTypeInfo *ti;
    int2dds_type_info_create("StringsType", 1, &ti);
    int2dds_type_info_add_field(ti, "id", INT2DDS_FIELD_INT32, 1);
    int2dds_type_info_add_field(ti, "unbounded_str", INT2DDS_FIELD_STRING, 0);
    int2dds_type_info_add_field(ti, "bounded_str", INT2DDS_FIELD_STRING, 0);
    return ti;
}

#endif /* STRINGS_IDL_H */
