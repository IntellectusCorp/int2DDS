/**
 * Auto-generated by int2dds-idl from PerformanceTestData.idl
 * DO NOT EDIT - changes will be overwritten
 */
#ifndef PERFORMANCE_TEST_DATA_IDL_H
#define PERFORMANCE_TEST_DATA_IDL_H

#include <stdbool.h>
#include <stdint.h>
#include <string.h>

#include "int2dds_cdr.h"

/* ========================================================================
 * Type: PerformanceTestData
 * Extensibility: FINAL
 * ======================================================================== */

typedef struct PerformanceTestData {
    uint64_t seq_num;
    uint64_t timestamp;
    struct { uint8_t* data; uint32_t length; } data;
} PerformanceTestData;

static inline size_t PerformanceTestData_serialize_cdr(
    const PerformanceTestData *val,
    uint8_t *buf,
    size_t capacity)
{
    Int2DdsCdrWriter w;
    int2dds_cdr_writer_init(&w, buf, capacity, true, true);
    int2dds_cdr_write_encapsulation(&w, INT2DDS_CDR_FINAL);
    int2dds_cdr_write_u64(&w, val->seq_num);
    int2dds_cdr_write_u64(&w, val->timestamp);
    int2dds_cdr_write_seq_header(&w, val->data.length);
    for (uint32_t _i = 0; _i < val->data.length; _i++) {
        int2dds_cdr_write_u8(&w, val->data.data[_i]);
    }
    return w.error == INT2DDS_CDR_OK ? int2dds_cdr_writer_size(&w) : 0;
}

static inline bool PerformanceTestData_deserialize_cdr(
    const uint8_t *buf,
    size_t len,
    PerformanceTestData *val_out)
{
    Int2DdsCdrReader r;
    if (int2dds_cdr_reader_init(&r, buf, len) != INT2DDS_CDR_OK)
        return false;
    int2dds_cdr_read_u64(&r, &val_out->seq_num);
    int2dds_cdr_read_u64(&r, &val_out->timestamp);
    int2dds_cdr_read_seq_header(&r, &val_out->data.length);
    for (uint32_t _i = 0; _i < val_out->data.length; _i++) {
        int2dds_cdr_read_u8(&r, &val_out->data.data[_i]);
    }
    return int2dds_cdr_reader_error(&r) == INT2DDS_CDR_OK;
}

static inline size_t PerformanceTestData_serialize_key(
    const PerformanceTestData *val,
    uint8_t *buf,
    size_t capacity)
{
    (void)val; (void)buf; (void)capacity;
    return 0;
}

#endif /* PERFORMANCE_TEST_DATA_IDL_H */
