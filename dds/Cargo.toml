[package]
name = "int2dds"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Rust implementation of the Data Distribution Service (DDS) middleware standard with RTPS protocol support"
keywords = ["dds", "rtps", "middleware", "pubsub", "realtime"]
categories = ["network-programming", "embedded", "api-bindings"]
documentation = "https://docs.rs/int2dds"
readme = "../README.md"

[features]
default = []
# Optional create_participant factory hook; off by default.
factory-hook = []

[dependencies]
int2dds-derive = { path = "../derive" }
mio = { version = "1.0.2", features = ["net", "os-poll"] }
network-interface = "2.0.0"
const-default = { version = "1.0.0", features = ["derive", "std", "alloc"] }
bitflags = "2.4"
speedy = "0.8.0"
bytes = "1"
smallvec = "1.13"
log = { version = "0.4.11", features = ["release_max_level_error"] }
env_logger = "0.11"
socket2 = { version = "0.5", features = ["all"] }
paste = { version = "1.0" }
chrono = "0.4.41"
uuid = { version = "1.17.0", features = ["v4"] }
strum = "0.27.1"
strum_macros = "0.27.1"
rand = "0.9.2"
regex = "1.0"
hostname = "0.4.1"
fern = "0.7.1"
md5 = "0.8"
roxmltree = "0.20"
clap = { version = "4.5", features = ["derive"] }
dashmap = "5.5"
arc-swap = "1.7"
libloading = "0.8"

num_cpus = "1.0"
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.21"
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "tls12", "logging"] }
tokio-util = { version = "0.7.18", features = ["rt"] }
futures-util = "0.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Successor of the unmaintained `get_if_addrs`, same authors and same OS calls
# (getifaddrs / GetAdaptersAddresses). Its Windows bindings are windows-sys
# instead of winapi 0.2.8, which has no aarch64 support.
# Keep the `link-local` feature off: disabled it drops fe80::/10 and 169.254/16
# exactly like `get_if_addrs` did.
if-addrs = "0.15"
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12", "logging"] }
rustls-pemfile = "2"
crossbeam-channel = "0.5.15"
flume = "0.11"

[target.'cfg(target_os = "linux")'.dependencies]
gpio-cdev = "0.6"

[lib]
crate-type = ["rlib"]
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
local-ip-address = "0.6.1"
winapi = { version = "0.3", features = [
    "processthreadsapi",
    "tlhelp32",
    "handleapi",
    "winnt",
    "winbase",
    "combaseapi",
    "errhandlingapi",
    "winerror",
    "libloaderapi",
    "memoryapi",
    "minwindef",
] }
# For getting Windows thread names in newer versions
windows-sys = { version = "0.52", features = ["Win32_Foundation", "Win32_System_Memory"] }

[[example]]
name = "hello_world_pub"
path = "examples/hello_world/hello_world_pub.rs"

[[example]]
name = "hello_world_sub"
path = "examples/hello_world/hello_world_sub.rs"

[dev-dependencies]
husky-rs = "0.1.5"
criterion = { version = "0.7.0", features = ["html_reports"] }
tempfile = "3"
ctrlc = { version = "3.4", features = ["termination"] }
rcgen = "0.13"

# Faster than opt-level = 0 and still reasonably debuggable
# opt-level = 1: keeps most of the code structure while applying light optimizations → much faster debugging
# opt-level = 2~3: too heavily optimized, making variable tracking difficult

[[bench]]
name = "reliable" # Name of the Rust source file where the benchmark code will be written
harness = false   # Set to false to disable the default benchmark functionality and enable Criterion

[[bench]]
name = "besteffort"
harness = false
